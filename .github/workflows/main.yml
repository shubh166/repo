name: CI Pipeline

on:
  push:
    branches: [main]

jobs:
 execute:
   runs-on: ubuntu-latest

   steps: 
     - name: Checkout
       uses: actions/checkout@v4.2.2

     - name: Setup Java JDK
       uses: actions/setup-java@v3.14.1

     - name: Run Java app
       run: Java Main.java
